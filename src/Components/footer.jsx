import React from 'react'

const footer = () => {
  const footerStyle = {
    textAlign: 'center',
    marginTop: '50px',
    borderTop: '1px solid #858383',
    paddingBlock: '20px',
  }

  return (
    <div style={footerStyle}>
      <p className="mb-0">Scandiweb Test assignment</p>
    </div>
  )
}

export default footer
